import 'package:flutter/material.dart';

class ArkSliderHome extends StatelessWidget {
  const ArkSliderHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
